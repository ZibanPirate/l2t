export const listToTree = (name: string) => {
  // if (name.length > 3) {
  //   return `Hi ${name}, That's a long-ass name!`;
  // } else {
  //   return `Hi ${name}`;
  // }
  return `Hi ${name}`;
};
